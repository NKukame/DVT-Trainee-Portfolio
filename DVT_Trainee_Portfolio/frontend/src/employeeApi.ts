
// frontend/src/api/employeeApi.ts

// import api from './index';
 

// export const getEmployees = () => api.get('/employees');
 

// export const getEmployeeById = (id: string) => api.get(`/employees/${id}`);



//  export const getEmployeeOnClient = (data: {
//   name: string;
//   position: string;
//   email: string;
// }) => api.post('/employees', data);


// export const createEmployee = (data: {
//   name: string;
//   position: string;
  // email: string;
// }) => api.post('/employees', data);
 


// export const updateEmployee = (id: string, data: Partial<{ name: string; position: string; email: string }>) =>
  // api.put(`/employees/${id}`, data);
 
// export const deleteEmployee = (id: string) => api.delete(`/employees/${id}`);

// JWT

// JWT functions

// var jwt = require('jsonwebtoken');
// var token = jwt.sign({ foo: 'bar' }, 'shhhhh'); results into a JWT token like this
//header.payload.signature


// Payload
{
  "studentNo": "1234567890",
  "name": "John Doe"
}

// Signature
// 


